import {CredixDividerDirective} from './credix-divider.directive';

describe('CredixDividerDirective', () => {
  it('should create an instance', () => {
    const directive = new CredixDividerDirective();
    expect(directive).toBeTruthy();
  });
});
