import {CredixFormFieldDirective} from './credix-form-field.directive';

describe('CredixFormFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new CredixFormFieldDirective();
    expect(directive).toBeTruthy();
  });
});
