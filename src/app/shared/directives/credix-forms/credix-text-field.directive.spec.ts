import {CredixTextFieldDirective} from './credix-text-field.directive';

describe('CredixInputTextDirective', () => {
  it('should create an instance', () => {
    const directive = new CredixTextFieldDirective();
    expect(directive).toBeTruthy();
  });
});
