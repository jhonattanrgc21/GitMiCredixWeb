import {CredixHeaderRowDirective} from './credix-header-row.directive';

describe('CredixHeaderRowDirective', () => {
  it('should create an instance', () => {
    const directive = new CredixHeaderRowDirective();
    expect(directive).toBeTruthy();
  });
});
