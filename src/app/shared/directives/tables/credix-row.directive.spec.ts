/*import {CredixRowDirective} from './credix-row.directive';

describe('CredixRowDirective', () => {
  it('should create an instance', () => {
    const directive = new CredixRowDirective();
    expect(directive).toBeTruthy();
  });
});*/
