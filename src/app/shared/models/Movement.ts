export class Movement {
  movementId: number;
  movementType: string;
  authorizationNumber: string;
  cardNumber: number;
  currencySimbol: string;
  establishmentName: string;
  originAmount: string;
  originAmountSign: string;
  originCurrency: string;
  originDate: string;
  quantifiableMovement: boolean;
  quota: number;
}
